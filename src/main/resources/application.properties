# MongoDB connection
spring.data.mongodb.uri=${MONGODB_URI:mongodb://localhost:27017/ecommerce}

# JWT configuration
# IMPORTANT: Replace this secret with a strong random string (min 32 chars) in production.
# Set via environment variable: JWT_SECRET=your-secret-here
jwt.secret=${JWT_SECRET:my-super-secret-key-for-dev-only-change-in-prod}
jwt.expiration-ms=${JWT_EXPIRATION_MS:86400000}

# Server
server.port=${PORT:8080}

# Disable Spring Security's default password generation (we manage auth ourselves)
spring.autoconfigure.exclude=org.springframework.boot.autoconfigure.security.servlet.UserDetailsServiceAutoConfiguration

# Logging
logging.level.learnMongoDb=INFO
